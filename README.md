# Ziggler.kz - Премиум платформа продажи мужских костюмов

## ⚠️ Важное уведомление
Проект имеет проблему с CSS компиляцией в production сборке. Для локальной разработки все работает корректно.

## 🔧 Переменные окружения

Для работы сайта и бота необходимо настроить следующие переменные окружения:

### Vercel Environment Variables:
```
MONGODB_URI=mongodb+srv://Vercel-Admin-ziggler:kfcQs6S4eLt0QGEK@ziggler.665067z.mongodb.net/?retryWrites=true&w=majority
TELEGRAM_BOT_TOKEN=8542343040:AAFf60s5j84QU33caha4JcxLhNbPMUrZU7A
NEXT_PUBLIC_BASE_URL=https://zigglerkz.vercel.app
NODE_ENV=production
```

### Как добавить переменные в Vercel:
1. Перейдите в проект на [vercel.com](https://vercel.com)
2. Откройте Settings → Environment Variables
3. Добавьте каждую переменную из списка выше
4. Передеплойте проект

Модерный E-commerce сайт для продажи премиум мужских костюмов с Telegram ботом.

## 🚀 Быстрый старт

### Запуск сайта и бота вместе

```bash
# Клонирование репозитория
git clone https://github.com/Allexndr/zigglerkz.git
cd zigglerkz

# Установка зависимостей
npm install

# Запуск сайта и настройка бота
./start-project.sh
```

Скрипт автоматически:
- 🚀 Запустит Next.js сайт на http://localhost:3000
- 🤖 Настроит Telegram бота через webhook API
- 📊 Проверит статус бота

### Ручной запуск

```bash
# Только сайт
npm run dev

# Настройка webhook бота
npm run bot:setup-webhook

# Проверка статуса бота
npm run bot:status
```

### Сборка для продакшена

```bash
npm run build
npm start
```

## 🧪 Тестирование

### Unit тесты (Jest)
```bash
npm test
npm run test:watch
npm run test:coverage
```

### E2E тесты (Playwright)
```bash
# Установка браузеров
npx playwright install

# Запуск всех E2E тестов
npm run test:e2e

# Запуск с UI
npm run test:e2e:ui

# Запуск всех тестов
./test-all.sh
```

### Покрытие тестами:
- ✅ **Unit тесты:** Компоненты, утилиты, хуки (32 теста)
- ✅ **E2E тесты:** Все страницы, функциональность
- ✅ **Интеграционные:** API, формы, навигация

## 📱 Функциональность

### Главная страница
- Hero секция с персонализацией
- Каталог коллекций
- Отзывы клиентов
- Newsletter подписка

### Каталог товаров
- Фильтры по цене, размеру, категории
- Поиск по товарам
- Сортировка
- Пагинация/бесконечная прокрутка

### Карточка товара
- Галерея изображений с зумом
- Выбор размера и цвета
- Количество товара
- Добавление в корзину/избранное
- Отзывы и рейтинг

### Корзина
- Управление количеством
- Удаление товаров
- Расчет итоговой суммы
- Промокоды
- Оформление заказа

## 🤖 Telegram бот

Бот полностью интегрирован с сайтом и работает через webhook API в Vercel.

### Возможности бота:
- 📦 **Каталог товаров** с пагинацией (5 товаров на страницу)
- 🛒 **Корзина** с управлением товарами и расчетом стоимости
- ❤️ **Избранное** - добавление/удаление товаров
- 👤 **Профиль** - настройки пользователя
- 📋 **Заказы** - история покупок
- ⚙️ **Настройки** - язык, уведомления

### Как работает бот:
1. **Webhook интеграция:** Бот получает обновления через `/api/bot/webhook`
2. **Единая база данных:** Использует тот же MongoDB Atlas, что и сайт
3. **Совместимость:** Те же товары, цены, категории

### Управление ботом:
```bash
# Настройка webhook
npm run bot:setup-webhook

# Удаление webhook
npm run bot:delete-webhook

# Проверка статуса
npm run bot:status
```

## 🎨 Дизайн система

### Цветовая палитра
- **Светлая тема:** Золотой акцент (#D4AF37) на сером фоне
- **Темная тема:** Элегантный navy с золотыми акцентами
- **Шрифты:** Inter + Playfair Display

### Компоненты
- Адаптивный дизайн (320px - 3840px)
- Доступность (WCAG 2.1 AA)
- Микроанимации
- Темная/светлая тема

## 🛠️ Технологии

- **Framework:** Next.js 14 (App Router)
- **Язык:** TypeScript
- **Стилизация:** Tailwind CSS
- **Тестирование:** Jest, React Testing Library, Playwright
- **Состояние:** Zustand
- **API:** TanStack Query
- **UI:** Radix UI, Lucide Icons

## 📁 Структура проекта

```
ziggler_website/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # Root layout
│   └── page.tsx           # Homepage
├── components/            # React компоненты
│   ├── layout/           # Layout компоненты
│   ├── sections/         # Страничные секции
│   ├── ui/              # Переиспользуемые UI
│   └── providers/       # Context providers
├── e2e/                  # E2E тесты (Playwright)
├── __tests__/            # Unit тесты (Jest)
├── hooks/                # Custom React hooks
├── lib/                  # Utilities и конфиг
└── styles/              # Global styles
```

## 🔧 Конфигурация

### Environment Variables
```env
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_BOT_USERNAME=@ziggler_bot
```

### Tailwind CSS
- Кастомные цвета и шрифты
- Темная тема через CSS classes
- Responsive breakpoints
- Custom animations

## 🚀 Деплой

### Vercel (рекомендуется)
```bash
npm run build
# Deploy to Vercel
```

### Docker
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]
```

## 📊 Метрики производительности

- **Lighthouse Score:** 95+ (Performance, Accessibility, SEO)
- **First Contentful Paint:** < 1.5s
- **Largest Contentful Paint:** < 2.5s
- **Cumulative Layout Shift:** < 0.1

## 🤝 Contributing

1. Fork the repository
2. Create feature branch (`git checkout -b feature/amazing-feature`)
3. Run tests (`./test-all.sh`)
4. Commit changes (`git commit -m 'Add amazing feature'`)
5. Push to branch (`git push origin feature/amazing-feature`)
6. Open Pull Request

## 📄 Лицензия

MIT License - см. [LICENSE](../LICENSE) файл.
